name: monexa
description: "MoneXa — CFO virtuel pour PME ouest-africaines. Application mobile Flutter offline-first pour la gestion de trésorerie Mobile Money."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State management
  flutter_bloc: ^8.1.6

  # Networking
  dio: ^5.7.0

  # Storage offline-first
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_secure_storage: ^9.2.2

  # Router
  go_router: ^14.2.7

  # i18n
  intl: ^0.19.0

  # UI
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.0

  # Image picker
  image_picker: ^1.1.2

  # Utilities
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
